import * as React from "react";

/**
 * Base UI element that has to be injected in the fragment.
 * @returns a react element
 */
export function FragmentElement() {
    return <div><%=fragmentName%> Fragment. Replace this with your UI.</div>
}