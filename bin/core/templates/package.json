{
    "name": "<%=name%>",
    "version": "1.0.0-alpha.0",
    "description": "<%=name%>",
    "main": "lib/<%=name%>.js",
    "types": "lib/<%=name%>.d.ts",
    "files": ["lib", "!/**/*.map", "!/**/*.json"],
    "scripts": {
        "link:shell": "npm link sg.shell",
        "link:toolkit": "npm link sg.plugin.toolkit",
        "link:login": "npm link sg.plugin.login",
        "link:dashboards": "npm link sg.plugin.dashboards",
        "link:grid": "npm link sg.plugin.grid",
        "link:charts": "npm link sg.plugin.charts",
        "link:all": "npm run link:shell && npm run link:login && npm run link:toolkit && npm run link:dashboards && npm run link:charts && npm run link:grid",
        "build:clear": "rimraf ./.temp && rimraf ./lib",
        "build:tsc": "npx tsc -p ./tsconfig.client.json",
        "build:tsc:watch": "npm run build:tsc -- -w --preserveWatchOutput",
        "build:bundle": "npx rollup -c",
        "build:bundle:watch": "npm run build:bundle -- -w --watch.buildDelay 500",
        "build:dts": "npx api-extractor run --local --verbose",
        "build:quick": "npm run build:tsc && npm run build:bundle",
        "build:watch": "npm-run-all -p build:tsc:watch build:bundle:watch",
        "build": "npm run build:clear && npm run build:tsc && npm run build:bundle && npm run build:dts"
    },
    "author": "<%=user%>",
    "license": "ISC",
    "devDependencies": {
        "@microsoft/api-extractor": "^7.9.2",
        "@rollup/plugin-commonjs": "^12.0.0",
        "@rollup/plugin-json": "^4.1.0",
        "@rollup/plugin-node-resolve": "^8.4.0",
        "@rollup/plugin-replace": "^2.3.2",
        "@types/papaparse": "^5.2.4",
        "@types/react": "16.9.35",
        "@types/react-dom": "^16.9.8",
        "@types/react-select": "^3.0.14",
        "npm-run-all": "^4.1.5",
        "react": "^16.13.1",
        "react-dom": "^16.13.1",
        "rimraf": "^3.0.2",
        "rollup": "^2.23.0",
        "rollup-plugin-terser": "^6.1.0",
        "sg.shell": "../output/sg.shell-1.10.3.tgz",
        "sg.plugin.toolkit": "../output/sg.plugin.toolkit-2.2.0.tgz",
        "sg.plugin.grid": "../output/sg.plugin.grid-2.0.1.tgz",
        "sg.plugin.dashboards": "../output/sg.plugin.dashboards-3.0.0.tgz",
        "sg.plugin.login": "../output/sg.plugin.login-3.1.0.tgz",
        "sg.plugin.charts": "../output/sg.plugin.charts-4.0.0.tgz",
        "typescript": "^3.9.7"
    },
    "dependencies": {}
}